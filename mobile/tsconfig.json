{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "//": "Jest specs are typechecked by tsconfig.jest.json (via ts-jest), which adds the jest/node types. Excluding them here keeps `npm run lint` (tsc --noEmit) scoped to app code.",
  "exclude": ["src/__tests__", "e2e"]
}
